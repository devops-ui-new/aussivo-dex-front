[build]
builder = "NIXPACKS"
buildCommand = "NODE_OPTIONS='--max-old-space-size=4096' npm ci --no-audit --no-fund && NODE_OPTIONS='--max-old-space-size=4096' npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
