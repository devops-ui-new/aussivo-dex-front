/** Shown in deposit modal, sidebar strip, and cancel-confirm dialog. */
export const DEPOSIT_STAY_WARNING =
  "Send only one on-chain transaction to this address — do not split the amount or send multiple transfers. Do not close this tab while funds are in flight. When we receive your transfer, this window closes automatically. Press Escape or Cancel only if you have not sent funds.";

/** Shown under “Send exactly” in the QR deposit modal. */
export const DEPOSIT_SINGLE_TX_HINT =
  "Use exactly one transfer to this address. Do not split or send a second transaction to top up.";
